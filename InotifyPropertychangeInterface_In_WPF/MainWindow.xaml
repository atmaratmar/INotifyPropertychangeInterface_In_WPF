<Window x:Class="InotifyPropertychangeInterface_In_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InotifyPropertychangeInterface_In_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Canvas>
        <!--Text="{Binding Path=_FistNmae,Mode=TwoWay}" binding to the ui-->
        <!--then we need set the data contex in the mainwindowsxamil.cs-->
        <Label Content="First Name" Canvas.Left="103" Canvas.Top="68"/>
        <TextBox Width="250" Text="{Binding Path=_FirstName,Mode=TwoWay}" Height="20" Canvas.Left="209" Canvas.Top="68"/>
        <Label Content="Last Name" Canvas.Left="100" Canvas.Top="107"/>
        <TextBox Width="250" Text="{Binding Path=_LastName,Mode=TwoWay}" Height="20" Canvas.Left="209" Canvas.Top="107"/>
        <Label Content="First Name" Canvas.Left="103" Canvas.Top="152"/>
        <TextBox Width="250" Text="{Binding Path=_FullName,Mode=TwoWay}" Height="20" Canvas.Left="209" Canvas.Top="152"/>
    </Canvas>
</Window>
